fastapi>=0.115.0
uvicorn[standard]>=0.30.0
# Pinned to match the Docker base image (mcr.microsoft.com/playwright/python
# :v1.45.0-jammy), which ships the matching browser build. If you bump this,
# bump the Dockerfile base image tag to the same version.
playwright==1.45.0
httpx>=0.27.0
pydantic>=2.8.0
python-dotenv>=1.0.1
